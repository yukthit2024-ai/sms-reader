create an android application which is a SMS manager
It should have a main screen with a list of SMS messages.
It need not be group by sender
It should have a search field.
on type of search term and submit, it should search and display matched results on same list
Develop the app in java 

Give it as a single prompt to give to an agent to achieve this
--------------------------------------------------------------------------------------------
more prompts
*	above the search text field, have a radio button "Group By Sender" and "Individual messages".
*	On clicking individual mesages, show messages in the list as single SMS messages.
	On clicking the "Group by sender",the folowing is expected....

*	If the third character in sender name is a "-", then ignore the first three character.  
		Then if the second last character is "-", then ignore the last two characters.  
		This gives me the "Sender name".   
		Now, group all messages with the same "sender name" into a single list 
		and show only the latest of that list in the main screen list

*	on the main screen.  when i click on the list item.... the folowing behaviour is expected....
		When in individual mode, show the full SMS message in a new screen.
		When in "Grouped" mode, clicking on one list item should take me to a list which shows all messages in that group in descending order of timestamp

*	on the main screen also, if in group mode, show the trimmed sender name.

*	in group mode , when a search is made... the main screen list shows matched messages grouped by Sender. this is fine.
		But when I click on the sender, it goes to second list... but this list contains all messages from that sender.  
		It should contain only search-criteria-matched messages from that sender.
		This behaviour should be maintained when a search criteria is present and user toggles between individual and group settings

*	Need a left side menu with items like "Settings", "Help", "About".
	for Settings and Help, place a screen with some dummy data
	The left side menu is covering from top to bottom.  Even when menu is visible, it should not cover the app top bar ....
	store the timestamp as ISO format instead of milliseconds since epoc
	have back arrow/buttons on each of the above pages

*	In the "About" screen, add build and git version details on the page.
	get git details from "X:\Projects_X\0_Active\8_Android_APK\Fake_GPS_Location_APK_GITHUB\app\build.gradle" and the below lines
       buildConfigField "String", "BUILD_TIMESTAMP", "\"${new Date().format('yyyyMMdd.HHmmss')}\""
       buildConfigField "String", "GIT_SHA", "\"${gitSha}\""
       buildConfigField "String", "GIT_SHA_FULL", "\"${gitShaFull}\""
       buildConfigField "String", "GIT_TAG", "\"${gitTag}\""		

*	make the search text field and search button same height
*	Once a messaged is opened and viewed, make the message as "read" status and do not highlight it anymore
*	after clicking to open an SMS message, update the SMS store to "read" status. it should not be highlighted anymore
*	Provide a option in the settings page to increase and decrease font sizes
*	in the message lists, put a border around messages, so that we can differentiate each message.
	Also when font size is changed, resize the list item size accordingly
*	remove millisecond from dattime display on screen.  Also show month as three char code
	example "yyyy-MMM-dd hh:mm:ss"
*	Apart from main screen, please provide a back arrow button on top left corner of all screens
*	on each message, provide a delete button, so that clciking on it will delete the relevant SMS from the underlying message store
*	the delete confirmation dialog is to be configurable in the settings.  If settings is for "No confirmation dialog", then delete the SMS directly
*	In settings, provide a button to open this app's setings.  If you can provide steps to make this app as the deafult SMS app, it would be even better.
*	But a border around the settings for "delete confirmation"
*	Multi-select items inside grouped messages
*	Once mutiple itmes are selcted, show options like "Select All", "Delete", to act on those selected messages
*	When "Select All" is selected and "Delete" done, it honors the "Confirm before delete" option.
	But If user select few rows manually and hits delete, it is still asking for confirmation, even though settings says not to ask for confirmation.
*	On left side menu, show export menu option
		The use can opt for XML or JSON or CSV export format.
		In settings, provide option for "Export folder" configuration
		Export file name format "SMS-yyyyMMdd.hhmmss.xml" or "SMS-yyyyMMdd.hhmmss.json", "SMS-yyyyMMdd.hhmmss.csv"
		xml format must be standard xml format for SMS exports used by other SMS exporting apps
		--
*	Give option to export in all three format by providing checkboxes for each.  By default all three checkboxes are to be selected
*	The "Export Folder" option is to be removed from general settings page and moved to "Export page".
*	Finally, On the export page, there should be Export folder settings, three export type checkboxes, followed by "Export" button
*	On clicking export, a Toast message show with message "Storage permission required for export".  Please take such permissions at start of the application

*	Save all settings at /sdcard/Vypeensoft/SMS_Manager/settings/settings.json
		The fields to save are 
		Font Size
		Confirm before deletion
		Export Folder
		Formats for export - 3 values.
		The export Folder should have default value of "/sdcard/Vypeensoft/SMS_Manager/export/"




Here’s a **single, well-scoped prompt** you can give to an AI coding agent to build your SMS manager app in Java:

---

## **PROMPT FOR AGENT**

Build a complete Android application in **Java (not Kotlin)** named **"SMS Manager"** with the following requirements.

---

## **App Overview**

* The app displays SMS messages from the device inbox
* Messages are shown in a simple list (no grouping by sender required)
* Includes a search feature to filter messages by keyword

---

## **Core Features**

### 1. Read SMS Messages

* Read SMS from device using:

  * `ContentResolver`
  * URI: `content://sms/inbox`
* Fetch and display:

  * Sender address
  * Message body
  * Timestamp
* Convert timestamp to readable format (e.g., `yyyy-MM-dd HH:mm:ss`)

---

### 2. SMS List Display

* Main screen shows all SMS messages in a **RecyclerView**
* Each item should display:

  * Sender
  * Message preview (first 1–2 lines)
  * Timestamp
* No grouping required

---

### 3. Search Functionality

* Add a **search input field** at top of screen
* Behavior:

  * User types search term and submits
  * Filter messages where:

    * Message body contains term OR
    * Sender contains term
* Display filtered results in same RecyclerView
* Provide option to clear search and show full list again

---

### 4. Permissions

Handle runtime permissions:

* `READ_SMS`

Behavior:

* If permission not granted:

  * Show explanation
  * Request permission
* Handle denial gracefully

---

## **User Interface**

### Main Screen Layout

* Top:

  * Search field (EditText)
  * Search button (or IME action)
* Below:

  * RecyclerView for SMS list

Optional:

* Empty state text ("No messages found")

---

## **Architecture & Code Structure**

* Use **Java + AndroidX**
* Suggested structure:

  * `MainActivity`
  * `SmsAdapter` (RecyclerView adapter)
  * `SmsModel` (data class)
  * `SmsRepository` or helper class for fetching SMS
* Follow clean and modular design

---

## **Performance Considerations**

* Load SMS asynchronously (background thread)
* Avoid blocking UI thread
* Use efficient filtering (in-memory list filtering)

---

## **Edge Cases**

Handle:

* No SMS available
* Permission denied
* Empty search results
* Large SMS dataset

---

## **Project Setup**

* Use **latest stable Gradle version**
* Include:

  * `gradlew`
  * `gradlew.bat`
  * `gradle-wrapper.properties`
  * `gradle.properties`

Ensure project builds with:

```
./gradlew build
```

---

## **Output Requirements**

Provide a **complete Android project**, including:

* Java source files
* XML layouts
* AndroidManifest.xml
* Gradle configuration files
* Fully runnable code (no pseudo code)

---

## **End of Prompt**
